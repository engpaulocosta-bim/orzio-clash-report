namespace OrzioClashReport.Core.Model
{
    /// <summary>Enumerates the only explicit human identity decisions allowed at this stage.</summary>
    public enum HumanIdentityDecisionKind
    {
        ConfirmSameIdentity = 0,
        RejectSameIdentity = 1,
    }
}
